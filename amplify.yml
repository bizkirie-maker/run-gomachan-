# AWS Amplify Hosting — ビルドなしの静的サイト（index.html / game.js / styles.css）
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo preBuild-ok
    build:
      commands:
        - echo build-ok
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
