# AWS Amplify Hosting — ビルドなしの静的サイト（index.html / game.js / styles.css）
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Static site: no npm"
    build:
      commands:
        - echo "Static site: no build step"
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
